React 组件通信与状态管理

组件通信：
    1、父组件向子组件通信：通过 props 传递数据。
    2、子组件向父组件通信：通过回调函数（props）实现。
    3、跨级组件通信：使用 Context API 或状态提升（lifting state up）。
    4、兄弟组件通信：通过共同的父组件通信。 状态提升
    5、全局状态管理：使用 Redux、MobX 等状态管理库。